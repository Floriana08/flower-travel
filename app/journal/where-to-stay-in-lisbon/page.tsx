import { redirect } from "next/navigation";

/** Suggested SEO slug; keep equity on the existing indexed article URL. */
export default function WhereToStayInLisbonRedirect() {
  redirect("/journal/where-to-stay-lisbon");
}
